# D-010 pre-epoch inputs

Status: input-only non-product research; `draft_unfrozen`

This directory contains only the candidate-neutral D-010 pre-epoch packet and
its eight-row case-input index. It prepares a strict, content-addressed boundary
for the owner-executable compiler-strategy comparison without freezing an
evidence epoch, accepting a prerequisite decision, admitting or installing a
toolchain, implementing a candidate adapter, or creating a candidate result.

Every CC-01 through CC-08 index row records absent shared inputs, absent
candidate mappings, zero executable fixtures, unresolved coverage, and an
active freeze blocker. The packet records accepted D-003 at exact revision
`a82a5cec2ee4359dc2fe66171f17c93146747333`, while D-004, D-005, D-006, and
D-009 acceptance remains absent. D-007, D-011, D-012, and D-013 remain downstream
decisions; bounded suite-only models may eventually exercise their interfaces
without accepting or predetermining them. Any external tool used in measured
work requires its applicable D-018 admission first.

The standard-library Rust laboratory may strictly parse these bytes, verify the
raw bindings to the index and unchanged
[`COMPILER_STRATEGY_DECISION_SUITE.md`](../../../docs/COMPILER_STRATEGY_DECISION_SUITE.md),
and enumerate the exact 40 candidate-case identities in memory. The inventory
is case-major and candidate-minor: CP-01 through CP-05 for CC-01, then the same
candidate order through CC-08. It is a canonical identity serialization, not an
authorized physical execution order.

The packet's strict canonical JSON SHA-256, excluding its final line feed, is
`9ec828033df99afa71ca88911484abfbfd007bd42538d8e02a5d7462413943b0`;
its raw file SHA-256, including the final line feed, is
`f081c3428c83a7be6d296317564adee0284d13f4913739e5cf33634ed748e799`.
Its closed packet schema is `d010-pre-epoch-packet-v0.1`, and the suite version
is `d010-v0.1-draft`. The case-input index's strict canonical JSON SHA-256 is
`4c8b0547a8f3bd380f4569008c8728014bb1d8718a5bfe17402bd03866560209`;
its raw file SHA-256, including the final line feed, is
`e9f59e86dff6219474d244ff01a98c75b7b17c65f1f91506d483a57e95e33670`.
The bound suite's raw SHA-256 is
`5d36f1faeda027b9784846af0aa742339c6b821f39b72a8ca067a90c41a46c73`.

The packet closes ten normalized semantic subjects under
`markdown-prose-lines-exact-v1`: the complete suite; the complete decision
register and exact D-010-to-D-011 range; the complete roadmap and exact
S5-to-S6 range; and the complete architecture, research, traceability,
assurance, and threat-model documents. Whole-document bindings prevent
competing language outside the bounded sections. Each bounded range includes
its exact start heading, excludes its exact unique successor heading, and
requires start-before-end order. Normalization preserves prose line,
indentation, and trailing-space structure while canonicalizing line endings and
the terminal line feed. These bindings do not classify English, accept a
decision, or create evidence.

The laboratory launches no process, writes no archive, evaluates no compiler or
toolchain output, validates no transformation, proof, certificate, leakage
trace, ABI, object, or final byte, and records no execution or evidence. All
candidate states remain absent or `not_performed`; resources, epoch, physical
order, selection, and conclusion remain null or unfrozen.

Current D-010 execution evidence remains 0/40 candidate-case runs. No compiler
strategy or output path is selected, preferred, recommended by this laboratory,
implemented, installed, or authorized for claim-bearing product work. This
preparation does not accept any prerequisite or downstream decision, close S5,
authorize a release, or advance Orange beyond 30% readiness.
